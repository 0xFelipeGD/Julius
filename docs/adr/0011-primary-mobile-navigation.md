# Primary mobile navigation

Julius 2.0 will use five primary mobile tabs: `Chat`, `Dashboard`, `Subscriptions`, `Statement`, and `Settings`. This preserves the existing mental model, promotes Subscriptions to a first-class surface, and avoids adding a new Today surface during the same release as the data-model and UI rework.
